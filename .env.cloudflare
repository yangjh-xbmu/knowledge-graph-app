# Cloudflare Pages 环境变量配置
# 此文件包含 Cloudflare Pages 部署时需要设置的环境变量

# ===========================================
# 必需的环境变量
# ===========================================

# Google Generative AI API 密钥
# 在 Cloudflare Pages 控制台中设置
GOOGLE_API_KEY=

# Kimi API 密钥 (月之暗面)
# 在 Cloudflare Pages 控制台中设置
KIMI_API_KEY=

# ===========================================
# Cloudflare 自动设置的环境变量
# ===========================================

# 以下变量由 Cloudflare Pages 自动设置，无需手动配置：
# CF_PAGES=1
# CLOUDFLARE_ENV=production (或 preview)
# CF_PAGES_COMMIT_SHA=<commit_hash>
# CF_PAGES_BRANCH=<branch_name>
# CF_PAGES_URL=<deployment_url>

# ===========================================
# 运行时配置
# ===========================================

# Node.js 环境
NODE_ENV=production

# Next.js 运行时
NEXT_RUNTIME=edge

# ===========================================
# 应用配置
# ===========================================

# 应用基础 URL (替换为你的域名)
NEXT_PUBLIC_APP_URL=https://your-app.pages.dev

# API 基础 URL
NEXT_PUBLIC_API_URL=https://your-app.pages.dev/api

# ===========================================
# 功能配置
# ===========================================

# 启用性能监控
ENABLE_ANALYTICS=true

# 禁用调试模式 (生产环境)
DEBUG=false

# 启用缓存
ENABLE_CACHE=true

# 缓存过期时间 (秒)
CACHE_TTL=3600

# ===========================================
# 安全配置
# ===========================================

# CORS 配置 (根据需要调整)
CORS_ORIGIN=https://your-domain.com

# API 超时时间 (毫秒)
API_TIMEOUT=30000

# ===========================================
# 日志配置
# ===========================================

# 日志级别
LOG_LEVEL=info

# 禁用详细日志 (生产环境)
VERBOSE_LOGGING=false

# ===========================================
# 部署说明
# ===========================================

# 1. 在 Cloudflare Pages 控制台中设置以上环境变量
# 2. 生产环境和预览环境可以设置不同的值
# 3. 敏感信息（如 API 密钥）请通过 Cloudflare Pages 控制台设置，不要提交到代码库
# 4. 确保所有必需的环境变量都已设置，否则应用可能无法正常工作

# ===========================================
# 环境变量设置步骤
# ===========================================

# 1. 登录 Cloudflare Dashboard
# 2. 进入 Pages 项目设置
# 3. 点击 "Settings" -> "Environment variables"
# 4. 分别为 Production 和 Preview 环境添加变量：
#    - GOOGLE_API_KEY: 你的 Google API 密钥
#    - KIMI_API_KEY: 你的 Kimi API 密钥
#    - NODE_ENV: production (生产环境) 或 development (预览环境)
#    - 其他根据需要添加的变量

# ===========================================
# 验证环境变量
# ===========================================

# 部署后可以通过以下 API 端点验证环境变量是否正确设置：
# GET /api/health - 检查应用健康状态
# POST /api/test-simple - 测试基本功能
# POST /api/test-ai - 测试 AI 功能（需要 API 密钥）